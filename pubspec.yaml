name: scheduled_health
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  ###
  # Core
  ###
  get_it: ^7.7.0
  path: ^1.9.0
  path_provider: ^2.1.3

  ###
  # Database & Storage
  ###
  sembast: ^3.7.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
